specVersion: "v1beta"
serviceId: "parlex"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "parlex-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
